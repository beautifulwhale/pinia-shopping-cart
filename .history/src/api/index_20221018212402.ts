/**
 * 
 * 模拟数据
 *
 */
import { resolveComponent } from "vue";
import { Product, product } from "../config";

function getProduct(){

}

function wait(delay:number){
    return new Promise((resolveComponent))
}